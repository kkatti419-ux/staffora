class AppHelpers {
  static int toInt(String value) {
    return int.tryParse(value) ?? 0;
  }
}

/*
Random utility functions like:
check internet
generate random ID
safe parse numbers
opening URLs 
*/